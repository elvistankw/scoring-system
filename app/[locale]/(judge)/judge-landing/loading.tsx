// Loading state for judge landing page
// Requirements: Device-based judge identity selection

import { DiaboloLoading } from '@/components/shared/diabolo-loading';

export default function JudgeLandingLoading() {
  return <DiaboloLoading message="加载评委入口..." />;
}